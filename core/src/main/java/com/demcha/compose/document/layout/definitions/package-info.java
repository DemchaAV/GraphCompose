/**
 * Built-in {@link com.demcha.compose.document.layout.NodeDefinition}
 * implementations split out of {@code BuiltInNodeDefinitions}.
 *
 * <p>This package is part of the Phase E.1 split (v1.6) that turns the
 * 3,000-line {@code BuiltInNodeDefinitions} catalogue into one file per
 * node type. {@code BuiltInNodeDefinitions.registerDefaults(NodeRegistry)}
 * remains the single registration entry point and instantiates the
 * implementations from this package.</p>
 */
@com.demcha.compose.document.api.Internal
package com.demcha.compose.document.layout.definitions;
