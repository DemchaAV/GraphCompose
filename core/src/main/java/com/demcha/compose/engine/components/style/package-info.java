/**
 * Reusable style components such as margin, padding, and color wrappers.
 *
 * <p>Ownership: Owned by both the canonical API and shared engine because style values cross that boundary.</p>
 * <p>Extension rules: Keep values small and immutable; do not add template-specific policy here.</p>
 */
package com.demcha.compose.engine.components.style;