/**
 * Internal font SPI shared by measurement and render backends.
 *
 * <p>Ownership: Owned by the shared engine/backend seam.</p>
 * <p>Extension rules: Extend only to support new backend font adapters; public font selection belongs in com.demcha.compose.font.</p>
 */
package com.demcha.compose.engine.font;