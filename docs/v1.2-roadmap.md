# GraphCompose v1.2 / v1.3 Roadmap

This document tracks the post-1.1 stabilization plan. v1.2 focused on cleaning up the canonical authoring path; v1.3 builds on top of it with new layout and export capabilities. Completed items reflect repository state; partial items remain release-polish work.

## v1.3 - capability increments (in progress)

The v1.3 release lands the first round of authoring features that close
remaining canonical-vs-legacy parity gaps without leaving the session-first API:

- horizontal rows: `addRow(...)` on flows, sections, and modules backed by
  `RowNode` + `RowBuilder`. Rows arrange atomic primitives left-to-right with
  optional `weights(...)` and `gap(...)`; the row is one atomic block from the
  paginator's perspective
- per-side borders: `DocumentBorders` value type plus `borders(...)` on flows,
  sections, modules, and rows. Per-side borders override the uniform stroke on
  the rectangle outline; each side renders independently
- auto-size text: `ParagraphBuilder.autoSize(maxSize, minSize)` searches for
  the largest font size that fits the paragraph on a single line within the
  resolved inner width
- session-level PDF chrome: `DocumentSession.metadata(...)`, `watermark(...)`,
  `protect(...)`, `header(...)`, `footer(...)`, and `clearHeadersAndFooters()`.
  `buildPdf`, `writePdf`, and `toPdfBytes` honour these options without an
  explicit `PdfFixedLayoutBackend.builder()` call
- functional DOCX backend: `DocxSemanticBackend` now produces real DOCX bytes
  via Apache POI (paragraphs, tables, images, spacers, page breaks, page
  geometry). Apache POI is declared optional, so PDF-only consumers do not pay
  the dependency cost. PPTX continues to ship as a manifest skeleton

Out of scope for v1.3 (carried into the next release):

- child horizontal/vertical alignment within rows and sections
- nested list builder (currently authored via sections)
- complex table cell composition (rich nodes inside cells)
- real PPTX export
- closing remaining `engine.*` allowlist leaks in `DocumentSession` / `font`

## v1.2 - stabilization plan

The v1.2 plan kept the existing engine cleaner to use, easier to test, and
safer to release. The roadmap is split into 10 phases. Completed items reflect
repository state; partial items remain release-polish work.

## Phase 0 - Release identity - Done

- [x] The next release ships as **v1.2.0** to match the project's current maturity and GitHub footprint.
- [x] `GraphCompose.document(...) -> DocumentSession -> DocumentDsl` is documented as the canonical authoring path.
- [x] Maven project coordinates use `io.github.demchaav:graphcompose:1.2.0`; JitPack uses `com.github.demchaav:GraphCompose:v1.2.0`.
- [x] Migration guide published at `docs/migration-v1-1-to-v1-2.md`.

## Phase 1 - Public API stabilization - In progress

Public packages:

- `com.demcha.compose`
- `com.demcha.compose.document.api`
- `com.demcha.compose.document.dsl`
- `com.demcha.compose.document.node`
- `com.demcha.compose.document.style`
- `com.demcha.compose.document.table`
- `com.demcha.compose.font`

Internal / advanced packages:

- `com.demcha.compose.engine.*`
- `com.demcha.compose.document.layout.*`
- `com.demcha.compose.document.backend.*`

Status:

- [x] README uses `GraphCompose.document(...)` as the public entry point.
- [x] `engine.*` is documented as internal / advanced, not the recommended authoring API.
- [x] `package-map.md` separates public and internal zones.
- [x] Runnable examples use the canonical public document API.
- [x] `DocumentSession` has closed-state lifecycle guards and idempotent `close()`.
- [x] Public DSL examples use `DocumentInsets`, `DocumentColor`, `DocumentStroke`, `DocumentTextStyle`, and `DocumentTable*`.
- [x] Remaining public `engine.*` allowlist leaks are being removed from `DocumentSession` and `font`.

## Phase 2 - Split `DocumentDsl` - Done

`DocumentDsl.java` is now a small facade. Focused builders live under `com.demcha.compose.document.dsl`:

```text
DocumentDsl.java
PageFlowBuilder.java
SectionBuilder.java
ModuleBuilder.java
ParagraphBuilder.java
ListBuilder.java
TableBuilder.java
ImageBuilder.java
ShapeBuilder.java
BarcodeBuilder.java
DividerBuilder.java
PageBreakBuilder.java
internal/
    BuilderSupport.java
    SemanticNameNormalizer.java
```

Current conversions are centralized in `document.layout.DocumentNodeAdapters`. The `document.dsl.internal` package currently contains builder and name helpers only.

## Phase 3 - Semantic layer purity - Done

- [x] Semantic nodes use renderer-neutral `DocumentLinkOptions`, `DocumentBookmarkOptions`, and `DocumentBarcodeOptions`.
- [x] PDF translation lives inside the fixed PDF backend/layout adapter layer.
- [x] Architecture guard tests keep `document.node.*` free of PDFBox imports.

## Phase 4 - Layout engine hardening - In progress

- [x] `BoxConstraints.UNBOUNDED_HEIGHT` centralizes the previous `1_000_000.0` sentinel.
- [x] `BoxConstraints.natural(width)` is the canonical factory for natural-height measurement.
- [x] `BoxConstraints.unboundedHeight(width)` remains as a compatibility alias.
- [x] Empty render throws a domain-specific `IllegalStateException`.
- [x] Pagination edge-case tests cover oversized atomic nodes, exact-fit content, leading/trailing page breaks, table row limits, nested sections, and module splits.

## Phase 5 - Rendering backend polishing - In progress

- [x] PDFBox code is isolated from `GraphCompose`, `document.api`, `document.dsl`, `document.node`, `document.layout`, and non-PDF backend contracts.
- [x] PDF metadata, protection, watermark, and header/footer options live behind `PdfFixedLayoutBackend.builder()`.
- [x] Debug guide-line overlays are available both through `PdfFixedLayoutBackend.builder()` and the session convenience path: `GraphCompose.document(...).guideLines(true)` / `DocumentSession.guideLines(true)`.
- [x] PDF render sessions own page-local content streams for the render pass.
- [x] Public font descriptors no longer materialize PDF/Word engine fonts directly.

## Phase 6 - Testing pyramid - In progress

- [x] Lifecycle tests cover closed-session and empty-document behavior.
- [x] Architecture guards cover public engine leaks, semantic PDFBox boundaries, and PDF backend isolation.
- [x] Layout snapshot and pagination tests cover the most important geometry cases.
- [x] Documentation/example tests render public README examples.
- [ ] Continue broadening public API compile coverage as new examples are added.

## Phase 7 - Performance and benchmarks - Done

- [x] Benchmark docs frame results as scenario-specific regression signals.
- [x] PR smoke and scheduled/manual full benchmark workflows exist.
- [x] JSON/CSV reports are produced as artifacts.
- [x] Median repeated-run mode is documented for local comparisons.

## Phase 8 - Documentation polish - Partially complete

- [x] README is short and focused on value, installation, quick start, concepts, and docs.
- [x] `docs/getting-started.md`, `docs/recipes.md`, `docs/architecture.md`, `docs/package-map.md`, `docs/lifecycle.md`, `docs/production-rendering.md`, `docs/layout-snapshot-testing.md`, `docs/benchmarks.md`, and `docs/release-process.md` exist.
- [x] Migration docs now target v1.1 to v1.2 canonical API adoption.
- [ ] Keep examples checked against documentation tests as new snippets are added.

## Phase 9 - Release packaging - Partially complete

- [x] groupId `io.github.demchaav`
- [x] artifactId `graphcompose`
- [x] version `1.2.0`
- [x] JitPack-first release checklist in `docs/release-process.md`
- [x] Runnable examples module builds separately through `examples/pom.xml`
- [x] Create local release tag `v1.2.0` after final verification.
- [x] GitHub draft release targets the current `v1.2.0` tag / commit.
- [ ] Verify JitPack against a fresh or rebuilt `v1.2.0` tag artifact; if the tag coordinate is stale, delete the cached JitPack build with authentication or ship a new patch tag.
- [x] Maven Central remains documented future work.

## Phase 10 - Portfolio / recruiter polish - Partially complete

Positioning:

> GraphCompose is a Java-first declarative document layout engine built on PDFBox, with semantic authoring, automatic pagination, deterministic layout snapshots, reusable templates, and a backend-oriented architecture designed for production document generation.

Pinned-repo description:

> Declarative Java document layout engine with semantic authoring, automatic pagination, PDFBox rendering, layout snapshots, and reusable templates.

Topics to keep or add:

- `java`
- `pdf`
- `pdf-generation`
- `document-generation`
- `layout-engine`
- `pdfbox`
- `report-generation`
- `template-engine`
- `declarative-ui`
- `templates`
- `pagination`
- `open-source`
