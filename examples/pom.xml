<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.demchaav</groupId>
        <artifactId>graph-compose-build</artifactId>
        <version>2.3.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>graph-compose-examples</artifactId>

    <name>GraphCompose Examples</name>
    <description>Runnable file-render examples for GraphCompose templates.</description>

    <properties>
        <graphcompose.version>${project.version}</graphcompose.version>
        <logback.version>1.6.3</logback.version>
        <maven.compiler.release>17</maven.compiler.release>

        <!--
            The version the committed previews under assets/readme/** were rendered
            at — the last release, not this tree. A preview carries the version in
            its coordinate pill, so a working tree already on the next version
            renders a different document by definition; CommittedAssetDriftTest can
            only compare like with like because the surefire configuration below
            pins the display version to this value.

            cut-release.ps1 moves it on a final cut, in the same commit as the tag and
            next to the step that re-renders the previews at that version. A
            pre-release cut leaves both alone: the version they would carry is this
            one's qualifier stripped, which names a release that does not exist yet.
        -->
        <graphcompose.examples.assetVersion>2.3.0</graphcompose.examples.assetVersion>

        <!--
            The date the committed previews print where an example demonstrates the
            {date} token. Pinned rather than "today" so a preview is a function of the
            code that renders it; the token itself still resolves from the clock for
            everybody else. GenerateAllExamples passes it below, so the release renders
            the same date the drift gate compares against.
        -->
        <graphcompose.examples.renderDate>2026-01-15</graphcompose.examples.renderDate>

        <junit.bom.version>6.1.3</junit.bom.version>
        <assertj.version>3.27.7</assertj.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit.bom.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>io.github.demchaav</groupId>
            <artifactId>graph-compose</artifactId>
            <version>${graphcompose.version}</version>
        </dependency>

        <!--
            Bundled Google fonts split out of the engine jar in v1.8.0. The
            engine no longer ships them, so the examples (which render CVs and
            showcases in those families) declare the companion artifact
            explicitly at its own independent version.
        -->
        <dependency>
            <groupId>io.github.demchaav</groupId>
            <artifactId>graph-compose-fonts</artifactId>
            <version>${graphcompose.fonts.version}</version>
        </dependency>

        <!--
            Colour-emoji glyphs for EmojiShortcodeExample. The engine resolves
            emoji from the classpath, so the examples declare the companion
            artifact explicitly at its own independent version (like fonts).
        -->
        <dependency>
            <groupId>io.github.demchaav</groupId>
            <artifactId>graph-compose-emoji</artifactId>
            <version>${graphcompose.emoji.version}</version>
        </dependency>

        <!-- DOCX export (WordExportExample): the semantic DOCX backend ships in
             graph-compose-render-docx since 2.0, which brings Apache POI
             transitively. -->
        <dependency>
            <groupId>io.github.demchaav</groupId>
            <artifactId>graph-compose-render-docx</artifactId>
            <version>${graphcompose.version}</version>
        </dependency>

        <!-- The fixed-layout PPTX backend: the Engine Deck PPTX example renders
             the flagship deck as a geometry-identical PowerPoint file. -->
        <dependency>
            <groupId>io.github.demchaav</groupId>
            <artifactId>graph-compose-render-pptx</artifactId>
            <version>${graphcompose.version}</version>
        </dependency>

        <!-- The built-in CV / cover-letter / invoice / proposal templates that the
             flagship + template examples render. -->
        <dependency>
            <groupId>io.github.demchaav</groupId>
            <artifactId>graph-compose-templates</artifactId>
            <version>${graphcompose.version}</version>
        </dependency>

        <!-- Snapshot-regression demo (LayoutSnapshotRegressionExample) uses
             LayoutSnapshotJson, which ships in graph-compose-testing since 2.0.
             It also brings jackson-databind transitively, which EngineDeckData
             uses to read the comparative-benchmark JSON — jackson left the
             engine jar in 2.0. -->
        <dependency>
            <groupId>io.github.demchaav</groupId>
            <artifactId>graph-compose-testing</artifactId>
            <version>${graphcompose.version}</version>
        </dependency>

        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
        </dependency>

        <!-- Test scope for the smoke runner. -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <!--
                Only banner.properties is filtered (it carries @project.version@
                so the README hero is stamped with the reactor version, never a
                hand-bumped literal). Every other resource is copied verbatim so
                binary assets (icons, fonts, sample data) stay byte-intact.
            -->
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
                <includes>
                    <include>banner.properties</include>
                </includes>
            </resource>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>false</filtering>
                <excludes>
                    <exclude>banner.properties</exclude>
                </excludes>
            </resource>
            <!--
                The committed perf baseline, read straight from where the gate
                keeps it. Examples that quote engine speed restate this file
                rather than carrying literals, and pointing at the original
                instead of copying it is what keeps the two from drifting.
            -->
            <resource>
                <directory>${project.basedir}/../baselines</directory>
                <targetPath>baselines</targetPath>
                <filtering>false</filtering>
                <includes>
                    <include>current-speed-full.json</include>
                </includes>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.15.0</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.6</version>
                <configuration>
                    <systemPropertyVariables>
                        <!--
                            Render at the version the committed previews carry, so a
                            test comparing against them compares two renders of one
                            document rather than this tree's version against the last
                            release's. Everything a test renders goes through the same
                            catalogue, so pinning it here pins all of it at once.
                        -->
                        <graphcompose.examples.displayVersion>
                            ${graphcompose.examples.assetVersion}
                        </graphcompose.examples.displayVersion>
                        <!--
                            And render at the date the previews carry. The chrome example
                            prints the {date} token, which the engine resolves from the clock,
                            so without this its committed preview differs from a fresh render
                            every day but the one it was committed on — a guard going red each
                            morning on a tree nobody touched.
                        -->
                        <graphcompose.renderDate>${graphcompose.examples.renderDate}</graphcompose.renderDate>
                    </systemPropertyVariables>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.6.3</version>
                <configuration>
                    <!--
                        GenerateAllExamples renders the committed previews, so it has to
                        pin the same date the tests compare them at. Set here rather than
                        on the command line: the release script invokes exec:java, and a
                        flag it forgets is a preview that drifts by a day.
                    -->
                    <systemProperties>
                        <systemProperty>
                            <key>graphcompose.renderDate</key>
                            <value>${graphcompose.examples.renderDate}</value>
                        </systemProperty>
                    </systemProperties>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>