<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
        Standalone, independently-versioned companion artifact carrying the
        bundled Google font binaries that used to live inside the main
        graph-compose jar (src/main/resources/fonts/google). Split out in the
        engine's v1.8.0 cycle so:
          * the engine artifact stays lean (no ~18 MB of fonts shipped on every
            engine release — and never duplicated into the engine -sources.jar);
          * fonts release on their OWN cadence — bumping the engine never drags
            a new fonts download to consumers, and re-publishing fonts is only
            needed when the font set itself changes.
        This module deliberately has NO Maven parent (so its version never tracks
        the engine line) and NO dependencies (it is a pure resource leaf; the
        font *descriptors* and the FontName API stay in the engine's
        com.demcha.compose.font package). The classpath layout fonts/google/...
        is preserved byte-for-byte, so the engine's DefaultFonts catalog resolves
        these resources unchanged once this jar is on the classpath.
    -->
    <groupId>io.github.demchaav</groupId>
    <artifactId>graph-compose-fonts</artifactId>
    <version>1.0.0</version>

    <name>GraphCompose Fonts</name>
    <description>Bundled Google font binaries for GraphCompose, packaged as an independently-versioned companion artifact.</description>
    <url>https://github.com/DemchaAV/GraphCompose</url>

    <licenses>
        <!--
            The font binaries under fonts/google/** are licensed under the SIL
            Open Font License (each family ships its own OFL.txt). This POM's
            own metadata follows the project's MIT license.
        -->
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
            <distribution>repo</distribution>
        </license>
        <license>
            <name>SIL Open Font License 1.1</name>
            <url>https://openfontlicense.org</url>
            <distribution>repo</distribution>
            <comments>Applies to the bundled font binaries under fonts/google/**; each family includes its own OFL.txt.</comments>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>DemchaAV</id>
            <name>Artem Demchyshyn</name>
            <email>demchishynartem@gmail.com</email>
            <url>https://github.com/DemchaAV</url>
            <roles>
                <role>Lead Developer</role>
                <role>Architect</role>
            </roles>
            <timezone>UTC 0</timezone>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/DemchaAV/GraphCompose.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/DemchaAV/GraphCompose.git</developerConnection>
        <url>https://github.com/DemchaAV/GraphCompose/tree/main</url>
    </scm>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>17</maven.compiler.release>

        <maven.compiler.plugin.version>3.15.0</maven.compiler.plugin.version>
        <maven.jar.plugin.version>3.5.0</maven.jar.plugin.version>
        <maven.source.plugin.version>3.4.0</maven.source.plugin.version>
        <maven.javadoc.plugin.version>3.12.0</maven.javadoc.plugin.version>
        <maven.gpg.plugin.version>3.2.8</maven.gpg.plugin.version>
        <central.publishing.plugin.version>0.10.0</central.publishing.plugin.version>

        <!-- See the engine pom: opted in via -Dgpg.skip=false on the publish workflow. -->
        <gpg.skip>true</gpg.skip>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven.compiler.plugin.version}</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>${maven.jar.plugin.version}</version>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
            Maven Central release artefacts — mirrors the engine pom's `release`
            profile (sources + javadoc + GPG signing + central-publishing). This
            module has no shared Maven parent by design, so the configuration is
            duplicated rather than inherited. Activated with `-P release`; the
            publish-fonts workflow flips -Dgpg.skip=false on a `fonts-v*` tag.
        -->
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <version>${maven.source.plugin.version}</version>
                        <executions>
                            <execution>
                                <id>attach-sources</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jar-no-fork</goal>
                                </goals>
                                <configuration>
                                    <!--
                                        Central requires a -sources.jar to exist,
                                        but there is no point shipping the ~17 MB
                                        of font binaries a SECOND time inside it.
                                        Exclude them so the sources jar carries
                                        only the real Java source (the marker
                                        class) and stays tiny.
                                    -->
                                    <excludes>
                                        <exclude>fonts/**</exclude>
                                    </excludes>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <version>${maven.javadoc.plugin.version}</version>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                                <configuration>
                                    <doclint>none</doclint>
                                    <failOnError>false</failOnError>
                                    <quiet>true</quiet>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>${maven.gpg.plugin.version}</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                                <configuration>
                                    <skip>${gpg.skip}</skip>
                                    <gpgArguments>
                                        <arg>--pinentry-mode</arg>
                                        <arg>loopback</arg>
                                    </gpgArguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>${central.publishing.plugin.version}</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <autoPublish>false</autoPublish>
                            <waitUntil>validated</waitUntil>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
