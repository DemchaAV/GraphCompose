<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
        The PDF render backend, split out of the engine in the 2.0 module line so
        the lean core carries no PDFBox. This module holds the fixed-layout PDF
        backend (document.backend.fixed.pdf.**), the PDF render tree
        (engine.render.pdf.**, incl. its fragment handlers), PdfFont /
        GlyphFallbackLogger, and the ServiceLoader registrations for the
        FixedLayoutBackendProvider + FontMetricsProvider seams. Adding this artifact
        (directly, or transitively via graph-compose) puts a PDF backend on the
        classpath so DocumentSession.buildPdf() / toImages() resolve; without any
        backend the core throws MissingBackendException.

        PDFBox, zxing (barcodes), and the commons-logging -> SLF4J bridge that
        PDFBox's static LogFactory init needs live here, not in the core.

        Standalone pom (no aggregator parent, like fonts/render-docx); its version
        tracks the engine line in lockstep (bumped by cut-release.ps1). It also
        builds a tests-classifier jar so graph-compose-qa can reuse the render test
        harness (EngineComposerHarness + the engine.assembly builders).
    -->
    <groupId>io.github.demchaav</groupId>
    <artifactId>graph-compose-render-pdf</artifactId>
    <version>2.2.1</version>

    <name>GraphCompose Render — PDF</name>
    <description>The PDFBox-backed PDF render backend for GraphCompose.</description>
    <url>https://github.com/DemchaAV/GraphCompose</url>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>DemchaAV</id>
            <name>Artem Demchyshyn</name>
            <email>demchishynartem@gmail.com</email>
            <url>https://github.com/DemchaAV</url>
            <roles>
                <role>Lead Developer</role>
                <role>Architect</role>
            </roles>
            <timezone>UTC 0</timezone>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/DemchaAV/GraphCompose.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/DemchaAV/GraphCompose.git</developerConnection>
        <url>https://github.com/DemchaAV/GraphCompose/tree/main</url>
    </scm>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>17</maven.compiler.release>

        <lombok.version>1.18.46</lombok.version>
        <pdfbox.version>3.0.8</pdfbox.version>
        <zxing.version>3.5.4</zxing.version>
        <slf4j.version>2.0.18</slf4j.version>
        <junit.bom.version>6.1.3</junit.bom.version>
        <assertj.version>3.27.7</assertj.version>
        <mockito.version>5.23.0</mockito.version>
        <logback.version>1.6.3</logback.version>
        <graphcompose.fonts.version>1.1.0</graphcompose.fonts.version>
        <graphcompose.emoji.version>1.0.0</graphcompose.emoji.version>

        <maven.compiler.plugin.version>3.15.0</maven.compiler.plugin.version>
        <maven.jar.plugin.version>3.5.0</maven.jar.plugin.version>
        <maven.surefire.plugin.version>3.5.6</maven.surefire.plugin.version>
        <!-- Keep in lockstep with the engine pom's jacoco.plugin.version
             (VersionConsistencyGuardTest enforces the equality). -->
        <jacoco.plugin.version>0.8.15</jacoco.plugin.version>
        <maven.source.plugin.version>3.4.0</maven.source.plugin.version>
        <maven.javadoc.plugin.version>3.12.0</maven.javadoc.plugin.version>
        <maven.gpg.plugin.version>3.2.8</maven.gpg.plugin.version>
        <central.publishing.plugin.version>0.11.0</central.publishing.plugin.version>

        <!-- See the engine pom: opted in via -Dgpg.skip=false on the publish workflow. -->
        <gpg.skip>true</gpg.skip>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>io.github.demchaav</groupId>
            <artifactId>graph-compose-core</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!--
            PDFBox's static initializer (PDDocument.<clinit>) calls
            org.apache.commons.logging.LogFactory directly. Exclude PDFBox's own
            commons-logging and route those calls through SLF4J via jcl-over-slf4j,
            so the whole train uses a single logging facade.
        -->
        <dependency>
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>pdfbox</artifactId>
            <version>${pdfbox.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>commons-logging</groupId>
                    <artifactId>commons-logging</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>jcl-over-slf4j</artifactId>
            <version>${slf4j.version}</version>
        </dependency>
        <dependency>
            <groupId>com.google.zxing</groupId>
            <artifactId>core</artifactId>
            <version>${zxing.version}</version>
        </dependency>

        <!-- Test dependencies. The render harness + backend tests build and
             rasterize real PDFs, so the bundled fonts / emoji are on the test
             classpath (like the engine's own test suite since v2.0). -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.bom.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.github.demchaav</groupId>
            <artifactId>graph-compose-fonts</artifactId>
            <version>${graphcompose.fonts.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.github.demchaav</groupId>
            <artifactId>graph-compose-emoji</artifactId>
            <version>${graphcompose.emoji.version}</version>
            <scope>test</scope>
        </dependency>
        <!--
            The engine's shared visual-test helper (com.demcha.testing.VisualTestOutputs)
            that a few backend render tests write their PDFs through. Pulled from the
            engine tests-classifier jar (a local build aid; the release profile disables it).
        -->
        <dependency>
            <groupId>io.github.demchaav</groupId>
            <artifactId>graph-compose-core</artifactId>
            <version>${project.version}</version>
            <type>test-jar</type>
            <classifier>tests</classifier>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven.compiler.plugin.version}</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven.surefire.plugin.version}</version>
            </plugin>
            <!--
                Coverage agent (prepare-agent only — no <argLine> here, so surefire
                picks up the property it writes). This module's exec feeds the
                report-aggregate in graph-compose-coverage.
            -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.plugin.version}</version>
                <executions>
                    <execution>
                        <id>jacoco-prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- Maven Central release artefacts — mirrors the engine pom's `release`
             profile (sources + javadoc + GPG + central-publishing). Standalone by
             design, so the configuration is duplicated rather than inherited. The
             tests-classifier jar is unbound (phase=none) so it is never deployed. -->
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <version>${maven.source.plugin.version}</version>
                        <executions>
                            <execution>
                                <id>attach-sources</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jar-no-fork</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <version>${maven.javadoc.plugin.version}</version>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                                <configuration>
                                    <doclint>none</doclint>
                                    <failOnError>false</failOnError>
                                    <quiet>true</quiet>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>${maven.gpg.plugin.version}</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                                <configuration>
                                    <skip>${gpg.skip}</skip>
                                    <gpgArguments>
                                        <arg>--pinentry-mode</arg>
                                        <arg>loopback</arg>
                                    </gpgArguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>${central.publishing.plugin.version}</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <autoPublish>false</autoPublish>
                            <waitUntil>validated</waitUntil>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
