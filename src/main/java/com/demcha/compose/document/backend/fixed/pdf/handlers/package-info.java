/**
 * Payload-specific PDF render handlers for the canonical semantic document backend.
 *
 * <p>Each handler owns one resolved fragment payload type and draws it onto the
 * page-scoped surface supplied by the surrounding PDF render session.</p>
 */
package com.demcha.compose.document.backend.fixed.pdf.handlers;
