/**
 * Canonical PDF option DTOs for the semantic {@code document.*} API.
 *
 * <p>The package defines immutable configuration objects used by
 * {@code GraphCompose.document(...)} and {@code DocumentSession} to describe
 * document-level PDF chrome and content-scoped PDF semantics without exposing
 * backend {@code layout_core} configuration types in the canonical public API.</p>
 */
package com.demcha.compose.document.backend.fixed.pdf.options;
