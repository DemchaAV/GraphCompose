/**
 * Backend extension seams for semantic GraphCompose documents.
 *
 * <p>Backends consume either resolved fixed-layout graphs or the semantic graph
 * directly, depending on whether the output format is page-absolute or
 * structure-oriented.</p>
 */
package com.demcha.compose.document.backend;
