/**
 * Semantic export backends that consume the document graph without fixed-layout rendering.
 *
 * <p>These backends are intended for structure-oriented formats such as DOCX or
 * PPTX where the future writer layer needs semantic nodes rather than
 * pre-resolved PDF coordinates.</p>
 */
package com.demcha.compose.document.backend.semantic;
