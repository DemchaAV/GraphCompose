/**
 * Public image source descriptors for the canonical document API.
 *
 * <p>Types in this package describe author intent only. They must not expose
 * internal engine image caches, render handlers, or PDF backend resources.</p>
 */
package com.demcha.compose.document.image;
