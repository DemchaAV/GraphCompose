/**
 * Fragment payload value types emitted by built-in
 * {@link com.demcha.compose.document.layout.NodeDefinition NodeDefinitions}
 * and consumed by PDF render handlers.
 *
 * <p>This package is part of the Phase E.2 split (v1.6) that pulls payload
 * records out of {@code BuiltInNodeDefinitions} into a dedicated subpackage.
 * Records here are tagged {@code @Internal} at the package level: their
 * fields and serialization shapes are implementation detail and may change
 * without notice between releases.</p>
 */
@com.demcha.compose.document.api.Internal
package com.demcha.compose.document.layout.payloads;
