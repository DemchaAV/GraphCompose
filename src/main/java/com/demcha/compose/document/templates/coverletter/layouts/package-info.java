/**
 * Superseded Gen-2 cover-letter layouts — frames that arrange a header
 * plus letter blocks (greeting, body paragraphs, closing) into a
 * final document tree.
 *
 * <p><strong>Deprecated surface.</strong> These are the older Gen-2
 * cover-letter layouts. They are <em>not</em> the current standard. The
 * current standard is the layered surface
 * {@code com.demcha.compose.document.templates.coverletter.v2} (data / theme /
 * components / widgets / presets). This package is kept only for backward
 * compatibility and is scheduled for removal in a future major.</p>
 *
 * <p>Currently a single layout
 * ({@link com.demcha.compose.document.templates.coverletter.layouts.LetterFormat})
 * covers all 14 cover-letter pair presets.</p>
 *
 * <p>New code should target the layered {@code coverletter.v2} surface
 * instead. See {@code docs/templates/v2-layered/}.</p>
 *
 * @since 1.6.0
 */
@Deprecated(since = "1.7.0", forRemoval = true)
package com.demcha.compose.document.templates.coverletter.layouts;
