/**
 * CV (résumé) template family.
 *
 * <p>The shipping CV surface is the layered stack under {@code cv.v2}
 * (data / theme / components / widgets / presets), built on the shared
 * {@code templates.core} layer and a {@code BrandTheme}.</p>
 *
 * @since 1.6.0
 */
package com.demcha.compose.document.templates.cv;
