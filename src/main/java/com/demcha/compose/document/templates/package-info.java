/**
 * Canonical reusable template layer built on top of the GraphCompose semantic
 * {@code document.*} API.
 *
 * <p>This package tree contains the public contracts, data objects, themes, and
 * built-in templates that compose through
 * {@link com.demcha.compose.document.api.DocumentSession}. The package is the
 * canonical template surface for V2 and replaces the older
 * {@code com.demcha.templates.*} namespace as the documented default.</p>
 */
package com.demcha.compose.document.templates;
