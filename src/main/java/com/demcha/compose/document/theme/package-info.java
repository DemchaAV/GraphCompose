/**
 * Design-token bundles ({@link com.demcha.compose.document.theme.BusinessTheme})
 * for business documents (invoice, proposal, report).
 *
 * <p>Tokens are defined entirely on canonical document values
 * (colors, insets, text styles, table cell styles) so authoring code stays
 * insulated from engine-level implementation details.</p>
 *
 * @author Artem Demchyshyn
 */
package com.demcha.compose.document.theme;
