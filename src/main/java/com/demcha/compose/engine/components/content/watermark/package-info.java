/**
 * Internal content components that carry backend-neutral payload data for renderable entities.
 *
 * <p>Ownership: Owned by the shared engine data model and consumed by measurement, layout, and render handlers.</p>
 * <p>Extension rules: Extend by adding immutable payload components and pairing them with explicit render/measurement behavior.</p>
 */
package com.demcha.compose.engine.components.content.watermark;