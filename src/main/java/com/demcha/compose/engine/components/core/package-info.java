/**
 * Core ECS identity and component storage primitives.
 *
 * <p>Ownership: Owned by the shared engine foundation and used by every low-level layout/render pipeline.</p>
 * <p>Extension rules: Keep this package minimal; new layout math belongs in engine helpers rather than on Entity itself.</p>
 */
package com.demcha.compose.engine.components.core;