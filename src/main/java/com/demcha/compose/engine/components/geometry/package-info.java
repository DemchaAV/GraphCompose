/**
 * Geometry components and helpers for content size, box size, and resolved entity bounds.
 *
 * <p>Ownership: Owned by the shared engine layout foundation.</p>
 * <p>Extension rules: Extend when the engine needs reusable geometry state; avoid backend-specific drawing logic here.</p>
 */
package com.demcha.compose.engine.components.geometry;