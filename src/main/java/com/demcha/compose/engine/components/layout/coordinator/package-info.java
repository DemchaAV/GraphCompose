/**
 * Layout components that describe anchors, parent links, layers, transforms, and resolved coordinates.
 *
 * <p>Ownership: Owned by the shared engine layout pipeline.</p>
 * <p>Extension rules: Extend with backend-neutral placement metadata only; renderers should consume this state, not define it.</p>
 */
package com.demcha.compose.engine.components.layout.coordinator;