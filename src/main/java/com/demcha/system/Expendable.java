package com.demcha.system;


/**
 * A marker interface used to identify entities that should expand their content size
 * to fit their children. The ContainerExpander system specifically looks for
 * entities with this component/interface to apply its expansion logic.
 */
public interface Expendable {
}
